// import WebSocket from 'ws';
// import { IShipsData } from '../../types/types';

// export const addShips = (
//   socket: WebSocket,
//   data: string | object,
//   clientMap: Map<WebSocket, number>,
// ) => {
//   const shipsData: IShipsData = JSON.parse(data.toString());
// };
